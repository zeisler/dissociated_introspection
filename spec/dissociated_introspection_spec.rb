require 'spec_helper'

describe DissociatedIntrospection do
  it 'has a version number' do
    expect(DissociatedIntrospection::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
